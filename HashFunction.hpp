#ifndef HASH_FUNCTION_HPP
#define HASH_FUCNTION_HPP

template <typename T>
class HashFunction {
    private:
        T key;
    public:
        T hash;
        T getHash(T key) {
            
        }
};

#endif